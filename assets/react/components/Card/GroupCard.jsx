import React from 'react';

import './Card.scss';

const GroupCard = ({group}) => {
    return (
        <div className="group-card">
            GroupCard
            {/* TODO : à finir + modification d'un groupe */}
        </div>
    );
}

export default GroupCard;